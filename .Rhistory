boxplot(grade ~ group, data = df,
main = "Grades by group",
xlab = "Group", ylab = "Grade")
stripchart(grade ~ group, data = df,
method = "jitter", vertical = TRUE,
add = TRUE, pch = 19)
boxplot(grade ~ group, data = df,
main = "Grades by group",
xlab = "Group", ylab = "Grade")
stripchart(grade ~ group, data = df,
method = "jitter", vertical = TRUE,
add = TRUE, pch = 19)
boxplot(grade ~ group, data = df,
main = "Grades by group",
xlab = "Group", ylab = "Grade")
stripchart(grade ~ group, data = df,
method = "jitter", vertical = TRUE,
add = TRUE, pch = 19)
library(ggplot2)
ggplot(df, aes(x = group, y = grade)) +
geom_boxplot(outlier.shape = NA) +
geom_jitter(width = 0.1, height = 0) +
labs(title = "Grades by group", x = "Group", y = "Grade")
par(mfrow = c(1, 3))
curve(dnorm(x, mean = 7, sd = 2), from = -1, to = 12,
main = expression(paste("Prior for ", beta[0])), xlab = expression(beta[0]), ylab = "density")
curve(dnorm(x, mean = 0, sd = 2), from = -6, to = 6,
main = expression(paste("Prior for ", beta[1])), xlab = expression(beta[1]), ylab = "density")
xs <- seq(0, 6, length.out = 500)
plot(xs, 2*dnorm(xs, mean = 0, sd = 2), type = "l",
main = expression(paste("Prior for ", sigma)), xlab = expression(sigma), ylab = "density")
par(mfrow = c(1, 1))
data {
stan_code <- '
data {
int<lower=1> N_boys;
int<lower=1> N_girls;
vector[N_boys] y_boys;
vector[N_girls] y_girls;
}
parameters {
real mu_boys;
real mu_girls;
real<lower=0> sigma_boys;
real<lower=0> sigma_girls;
}
model {
// priors
mu_boys ~ normal(7, 2);
mu_girls ~ normal(7, 2);
sigma_boys ~ normal(0, 2);
sigma_girls ~ normal(0, 2);
// likelihood
y_boys ~ normal(mu_boys, sigma_boys);
y_girls ~ normal(mu_girls, sigma_girls);
}
generated quantities {
real delta;
real y_new_girl;
delta = mu_boys - mu_girls;
y_new_girl = normal_rng(mu_girls, sigma_girls);
}
'
writeLines(stan_code, "bayesian_grades_model.stan")
cat(stan_code)
library(rstan)
library(bayesplot)
library(posterior)
rstan_options(auto_write = TRUE)
options(mc.cores = parallel::detectCores())
stan_data <- list(
N_boys  = length(boys),
N_girls = length(girls),
y_boys  = boys,
y_girls = girls
)
fit <- stan(
file = "bayesian_grades_model.stan",
data = stan_data,
chains = 4,
iter = 4000,
warmup = 2000,
seed = 1234,
refresh = 0
)
```{r stan-fit, eval=TRUE}
library(rstan)
library(bayesplot)
library(posterior)
rstan_options(auto_write = TRUE)
options(mc.cores = parallel::detectCores())
stan_data <- list(
N_boys  = length(boys),
N_girls = length(girls),
y_boys  = boys,
y_girls = girls
)
fit <- stan(
file = "bayesian_grades_model.stan",
data = stan_data,
chains = 4,
iter = 4000,
warmup = 2000,
seed = 1234,
refresh = 0
)
fit <- stan(
file = "bayesian_grades_model.stan",
data = stan_data,
chains = 4,
iter = 4000,
warmup = 2000,
seed = 1234,
refresh = 0
)
print(fit, pars = c("mu_boys", "mu_girls", "sigma_boys", "sigma_girls", "delta", "y_new_girl"),
probs = c(0.025, 0.5, 0.975))
fit <- stan(
file = "bayesian_grades_model.stan",
data = stan_data,
chains = 4,
iter = 4000,
warmup = 2000,
seed = 1234,
refresh = 0
)
knitr::opts_chunk$set(echo = TRUE)
library(cluster)  # PAM
library(clustEff) # tess
library(clustEff) # tess
library(monoClust) #  pseudo-f & sil
library(vegan) # distances and methods
library(ade4)
library(readxl)
x <- matrix(rnorm(100), nrow = 5)
View(x)
View(x)
dim(x)
dist(x)
? dist
dist(x, diag = TRUE)
dist(x, upper = TRUE)
m <- as.matrix(dist(x))
m
x <- c(1, 2, 3, 4, 5)
y <- c(2, 1, 3, 5, 4)
tau <- cor(x, y, method = "kendall")
dist <- (1 - tau) / 2
tau
dist
# use vegan package to compute the distance using hamming of random generated matrix
set.seed(123)
random_matrix <- matrix(sample(0:1, 100, replace = TRUE), nrow)
# use vegan package to compute the distance using hamming of random generated matrix
set.seed(123)
random_matrix <- matrix(sample(0:1, 100, replace = TRUE), nrow = 10)
hamming_distance <- vegdist(random_matrix, method = "hamming")
hamming_distance <- vegdist(random_matrix, method = "humming")
hamming_distance <- vegdist(random_matrix, method = "hellinger")
hamming_distance
setwd("C:/Users/Leonel/Desktop/Universidad/MESIO/Bayesian-Analysis/Project/Analisis-Bayesiano")
# rstan libraries
library(rstan)
options(mc.cores = parallel::detectCores())
# impor dataset from the folder
df <- read.csv("data/arrivals_2026-05-02_8_2.csv")
View(df)
View(df)
library(tidyverse)
library(lubridate)
library(cmdstanr)
library(posterior)
# 1) Preprocesado: eventos únicos de llegada
arrivals <- df %>%
mutate(
captured_at = ymd_hms(captured_at, tz = "UTC"),
arrival_time = as_datetime(temps_arribada / 1000, tz = "UTC"),
time_block = hour(arrival_time)   # momento del día: hora
) %>%
group_by(codi_parada, nom_parada, sentit, id_bus, arrival_time) %>%
summarise(
ordre = first(ordre),
time_block = first(time_block),
.groups = "drop"
)
View(arrivals)
View(arrivals)
df_2 <- read.csv("eventos_nuevos/arrivals_2026-05-02_8_2_limpio_3.csv")
View(df_2)
View(df_2)
# impor dataset from the folder
df <- read.csv("eventos_nuevos/arrivals_2026-05-02_8_2_limpio_3.csv")
headways <- df %>%
filter(!is.na(headway_min), headway_min > 0) %>%
mutate(
stop_id = as.integer(factor(paste(sentit, codi_parada))),
time_id = as.integer(factor(hora_paso))
)
stan_data <- list(
N = nrow(headways),
S = max(headways$stop_id),
Tt = max(headways$time_id),
stop = headways$stop_id,
time = headways$time_id,
H = headways$headway_min
)
mod <- cmdstan_model("models_stan/headway_model.stan")
fit <- stan(
"models_stan/headway_model.stan",
data = stan_data,
chains = 4,
parallel_chains = 4,
iter_warmup = 1000,
iter_sampling = 1000,
seed = 123,
adapt_delta = 0.95
)
fit <- stan(
"models_stan/headway_model.stan",
data = stan_data,
chains = 4,
parallel_chains = 4,
iter_warmup = 1000,
iter_sampling = 1000,
seed = 123,
adapt_delta = 0.95
)
fit <- rstan::stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
fit <- rstan::stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
print(fit, pars = c("alpha", "sigma_stop", "sigma_time", "kappa"))
fit <- rstan::stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
Sys.which("make")
fit <- rstan::stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
fit <- rstan::stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
fit <- stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
Sys.getenv("BINPREF")
writeLines('PATH="${RTOOLS40_HOME}\\usr\\bin;${PATH}"', con = "~/.Renviron")
setwd("C:/Users/Leonel/Desktop/Universidad/MESIO/Bayesian-Analysis/Project/Analisis-Bayesiano")
# rstan libraries
library(rstan)
options(mc.cores = parallel::detectCores())
# impor dataset from the folder
df <- read.csv("eventos_nuevos/arrivals_2026-05-02_8_2_limpio_3.csv")
headways <- df %>%
filter(!is.na(headway_min), headway_min > 0) %>%
mutate(
stop_id = as.integer(factor(paste(sentit, codi_parada))),
time_id = as.integer(factor(hora_paso))
)
library(dplyr)
library(tidyverse)
headways <- df %>%
filter(!is.na(headway_min), headway_min > 0) %>%
mutate(
stop_id = as.integer(factor(paste(sentit, codi_parada))),
time_id = as.integer(factor(hora_paso))
)
stan_data <- list(
N = nrow(headways),
S = max(headways$stop_id),
Tt = max(headways$time_id),
stop = headways$stop_id,
time = headways$time_id,
H = headways$headway_min
)
fit <- stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
fit <- stan(
file = "models_stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = parallel::detectCores(),
iter = 2000,
warmup = 1000,
seed = 123,
control = list(adapt_delta = 0.95)
)
remove.packages(c("rstan", "StanHeaders"))
install.packages(
c("StanHeaders", "rstan"),
repos = c("https://stan-dev.r-universe.dev", "https://cloud.r-project.org")
)
library(tidyverse)
library(rstan)
rstan_options(auto_write = TRUE)
options(mc.cores = 4)
Y_SHIFT <- 0.16
HW_MIN <- 1
HW_MAX <- 30
DATA_DIR <- "processed/headways/"
hora_a_franja <- function(hora) {
ifelse(hora <= 6, 0,
ifelse(hora <= 21, hora - 6, 16))
}
files_info <- tibble(
file = list.files(
DATA_DIR,
pattern = "^headways_pares3_H8_Anada_\\d{4}-\\d{2}-\\d{2}.*\\.csv$",
full.names = TRUE
)
) %>%
mutate(
fecha = as.Date(str_extract(basename(file), "\\d{4}-\\d{2}-\\d{2}")),
weekday = as.integer(format(fecha, "%u")) # lunes=1, ..., domingo=7
) %>%
arrange(fecha) %>%
group_by(weekday) %>%
slice_head(n = 2) %>%
ungroup()
files <- files_info$file
files_info %>%
select(fecha, weekday, file)
df_all <- map_dfr(files, leer_headway)
library(tidyverse)
library(rstan)
rstan_options(auto_write = TRUE)
options(mc.cores = 4)
Y_SHIFT <- 0.16
HW_MIN <- 1
HW_MAX <- 30
DATA_DIR <- "processed/headways/"
hora_a_franja <- function(hora) {
ifelse(hora <= 6, 0,
ifelse(hora <= 21, hora - 6, 16))
}
files_info <- tibble(
file = list.files(
DATA_DIR,
pattern = "^headways_pares3_H8_Anada_\\d{4}-\\d{2}-\\d{2}.*\\.csv$",
full.names = TRUE
)
) %>%
mutate(
fecha = as.Date(str_extract(basename(file), "\\d{4}-\\d{2}-\\d{2}")),
weekday = as.integer(format(fecha, "%u")) # lunes=1, ..., domingo=7
) %>%
arrange(fecha) %>%
group_by(weekday) %>%
slice_head(n = 2) %>%
ungroup()
files <- files_info$file
files_info %>%
select(fecha, weekday, file)
df_all <- purrr::map_dfr(files, function(f) {
fecha <- stringr::str_extract(basename(f), "\\d{4}-\\d{2}-\\d{2}")
read.csv(f) %>%
mutate(
source_file = basename(f),
fecha = as.Date(fecha)
)
})
stan_data <- list(
N = nrow(headways),
S = max(headways$stop),
Tt = max(headways$time),
stop = headways$stop,
time = headways$time,
H = headways$H
)
library(tidyverse)
library(rstan)
rstan_options(auto_write = TRUE)
options(mc.cores = 4)
Y_SHIFT <- 0.16
HW_MIN <- 1
HW_MAX <- 30
DATA_DIR <- "processed/headways/"
hora_a_franja <- function(hora) {
ifelse(hora <= 6, 0,
ifelse(hora <= 21, hora - 6, 16))
}
files_info <- tibble(
file = list.files(
DATA_DIR,
pattern = "^headways_pares3_H8_Anada_\\d{4}-\\d{2}-\\d{2}.*\\.csv$",
full.names = TRUE
)
) %>%
mutate(
fecha = as.Date(str_extract(basename(file), "\\d{4}-\\d{2}-\\d{2}")),
weekday = as.integer(format(fecha, "%u")) # lunes=1, ..., domingo=7
) %>%
arrange(fecha) %>%
group_by(weekday) %>%
slice_head(n = 2) %>%
ungroup()
files <- files_info$file
files_info %>%
select(fecha, weekday, file)
df_all <- map_dfr(files, leer_headway)
setwd("C:/Users/leone/Desktop/MESIO/Bayesian-Analysis/Analisis-Bayesiano")
df_all <- map_dfr(files, leer_headway)
leer_headway <- function(f) {
fecha <- str_extract(
basename(f),
"\\d{4}-\\d{2}-\\d{2}"
)
read.csv(f) %>%
mutate(
source_file = basename(f),
fecha = as.Date(fecha)
)
}
df_all <- map_dfr(files, leer_headway)
headways <- df_all %>%
filter(
!is.na(headway_pair),
headway_pair >= HW_MIN,
headway_pair <= HW_MAX
) %>%
mutate(
weekday = as.integer(format(fecha, "%u")),  # lunes=1, domingo=7
franja = hora_a_franja(as.integer(franja_hora)),
H = headway_pair - Y_SHIFT
) %>%
filter(H > 0) %>%
mutate(
stop = as.integer(factor(codi_parada)),
time = as.integer(factor(paste(weekday, franja, sep = "_")))
)
stan_data <- list(
N = nrow(headways),
S = max(headways$stop),
Tt = max(headways$time),
stop = headways$stop,
time = headways$time,
H = headways$H
)
table(headways$fecha)
table(headways$weekday)
length(unique(headways$stop))
length(unique(headways$time))
summary(headways$H)
fit <- rstan::stan(
file = "bayesian/R/stan/headway_model.stan",
data = stan_data,
chains = 4,
cores = 4,
iter = 2000,
warmup = 500,
seed = 123,
control = list(adapt_delta = 0.99),
refresh = 100
)
print(fit, pars = c("alpha", "sigma_stop", "sigma_time", "kappa"))
saveRDS(fit, file.path("bayesian/R/stan/fits", "headway_model_fit.rds"))
saveRDS(fit, file.path("bayesian/R/stan/fits", "headway_model_fit.rds"))
saveRDS(fit, file.path("bayesian/R/stan/fits", "headway_model_fit.rds"))
getwd()
saveRDS(fit, file.path("bayesian/R/stan/fits", "headway_model_fit.rds"))
